@model Emina.Models.Answer

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Answer</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.EnqueteID, "Enquete")
        </div>
        <div class="editor-field">
            @Html.DropDownList("EnqueteID", String.Empty)
            @Html.ValidationMessageFor(model => model.EnqueteID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.QuestionID, "Question")
        </div>
        <div class="editor-field">
            @Html.DropDownList("QuestionID", String.Empty)
            @Html.ValidationMessageFor(model => model.QuestionID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PossibleAnswerID, "PossibleAnswer")
        </div>
        <div class="editor-field">
            @Html.DropDownList("PossibleAnswerID", String.Empty)
            @Html.ValidationMessageFor(model => model.PossibleAnswerID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
